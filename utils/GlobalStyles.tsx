import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  imageWaifu: {
    width: 150,
    height: 300,
    borderRadius: 10,
    resizeMode: 'cover',
  }
});

export default styles;