import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  imageWaifu: {
    width: 150,
    height: 300,
    borderRadius: 10,
    resizeMode: 'cover',
    borderWidth: 2,
    borderColor: '#6200ee'
  }
});

export default styles;